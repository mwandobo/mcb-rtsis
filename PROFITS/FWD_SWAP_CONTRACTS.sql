create table FWD_SWAP_CONTRACTS
(
    REFERENCE_NO       INTEGER,
    PRINT_STATUS       SMALLINT,
    TARGET_AVAIL_DAYS  SMALLINT,
    TARGET_VALUE_DAYS  SMALLINT,
    SOURCE_VALUE_DAYS  SMALLINT,
    SPOT_TUN_TRX_UNIT  INTEGER,
    FK0CURRENCYID_CURR INTEGER,
    FK_CURRENCYID_CURR INTEGER,
    FK_UNITCODE        INTEGER,
    FK_PRODUCTID_PRODU INTEGER,
    FK_CUSTOMERCUST_ID INTEGER,
    SPOT_TUN_TRX_SN    INTEGER,
    FK0DEPOSIT_ACCOACC DECIMAL(11),
    FK_DEPOSIT_ACCOACC DECIMAL(11),
    CURRENCY_RATE      DECIMAL(12, 6),
    TARGET_UTIL_BAL    DECIMAL(15, 2),
    ORG_TARGET_AMOUNT  DECIMAL(15, 2),
    SOURCE_UTIL_BAL    DECIMAL(15, 2),
    ORG_SOURCE_AMOUNT  DECIMAL(15, 2),
    UTILISED_BALANCE   DECIMAL(15, 2),
    MATURITY_DATE      DATE,
    SOURCE_VALUE_DATE  DATE,
    CONTRACT_DATE      DATE,
    TARGET_VALUE_DATE  DATE,
    START_DATE         DATE,
    NOTIFICATION_DATE  DATE,
    EXEC_DATE          DATE,
    TARGET_AVAIL_DATE  DATE,
    TMSTAMP            TIMESTAMP(6),
    SPOT_TUN_TRX_DATE  DATE,
    TRX_DATE           DATE,
    WAY_OF_UTILIZATION CHAR(1),
    ENTRY_STATUS       CHAR(1),
    SPOT_TUN_TRX_USR   CHAR(8),
    FK_USRCODE         CHAR(8),
    DEALER_REF_NO      CHAR(20),
    FK0GLG_ACCOUNTACCO CHAR(21),
    FK_GLG_ACCOUNTACCO CHAR(21),
    ENTRY_COMMENTS     VARCHAR(120)
);

create unique index IXU_FWD_000
    on FWD_SWAP_CONTRACTS (REFERENCE_NO);

