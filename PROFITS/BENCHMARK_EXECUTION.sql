create table BENCHMARK_EXECUTION
(
    EXECUTION_SN      DECIMAL(15) not null,
    BENCHMARK_ID      DECIMAL(15) not null,
    BENCHMARK_SN      DECIMAL(15) not null,
    EXECUTION_CNT_SET INTEGER,
    EXECUTION_TOTAL   INTEGER,
    EXECUTION_DATE    DATE,
    EXECUTION_USER    CHAR(8),
    TMSTAMP_START     TIMESTAMP(6),
    TMSTAMP_END       TIMESTAMP(6),
    SYSTEM_GROUP      VARCHAR(50),
    SYSTEM_USER       VARCHAR(50),
    SYSTEM_TERMINAL   CHAR(100),
    SYSTEM_DATE       DATE,
    BENCHMARK_STATUS  CHAR(1),
    RUNTIME_MESSAGE   CHAR(80),
    DELAY_TIMER       INTEGER,
    constraint IXU_BNC_002
        primary key (EXECUTION_SN, BENCHMARK_ID, BENCHMARK_SN)
);

